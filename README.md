# Flask DevOps Lab

## Usage

```bash
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

### Endpoints

- `/api/health` - Returns application health status.
- `/api/config` - Returns the current application configuration.
- `/api/report` - Returns hostname, Python version, and uptime information.
